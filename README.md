# 💰 SB Finance AI - Финансовый помощник для малого бизнеса

[![Python](https://img.shields.io/badge/Python-3.9+-blue.svg)](https://www.python.org/)
[![Django](https://img.shields.io/badge/Django-5.0-green.svg)](https://www.djangoproject.com/)
[![License](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

**SB Finance AI** — это современный веб-сервис для управления финансами малого бизнеса с интеллектуальной аналитикой, AI-рекомендациями, автоматической категоризацией расходов и продвинутой системой импорта данных.

## ✨ Основные возможности

### 📊 Финансовый учет
- **Доходы и расходы** с категоризацией и тегами
- **Календарь событий** для важных финансовых дат
- **Генерация документов** (договоры, счета, акты)
- **Интерактивный дашборд** с графиками и аналитикой

### 🤖 AI и Machine Learning
- **AI-чат** с финансовым консультантом на базе OpenRouter
- **Автоматическая категоризация расходов** (ML-классификатор)
- **Прогнозирование прибыли** на основе исторических данных
- **Умные рекомендации** по оптимизации финансов
- **Автоматическое обнаружение аномалий** в тратах

### 📁 Импорт и управление данными
- **Импорт CSV/Excel** файлов с транзакциями
- **Отслеживание источников** — каждая транзакция привязана к файлу
- **Автоматическое обнаружение дубликатов** при импорте
- **Фильтрация по файлам** в списках доходов/расходов
- **Массовое удаление** транзакций по файлам
- **Быстрая очистка дубликатов** с визуальным интерфейсом

### 🔒 Приватность и безопасность
- **Клиентское шифрование** данных (AES-256-GCM)
- **Анонимизация данных** перед отправкой в облако
- **Локальный режим** с поддержкой Ollama
- **Экспорт зашифрованных данных** в `.sbenc` формат
- **Полное удаление данных** одной кнопкой

## 🚀 Быстрый старт

### Требования
- Python 3.9+
- pip
- (Опционально) Ollama для локального режима AI

### Установка

1. **Клонируйте репозиторий**
```bash
git clone https://github.com/yourusername/sb-finance-ai.git
cd sb-finance-ai
```

2. **Создайте виртуальное окружение**
```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

3. **Установите зависимости**
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения**

Создайте файл `.env` в корне проекта:
```env
# Django настройки
DJANGO_SECRET_KEY=your-secret-key-here-change-this
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# OpenRouter AI (получите ключ на https://openrouter.ai/keys)
LLM_API_KEY=sk-or-v1-your-key-here
LLM_API_URL=https://openrouter.ai/api/v1/chat/completions
# БЕСПЛАТНЫЕ МОДЕЛИ: deepseek-chat-v3.1:free, qwen3-coder-480b-a35b:free
# ПЛАТНЫЕ МОДЕЛИ: openai/gpt-4o-mini, google/gemini-pro-1.5-flash, и др.
LLM_MODEL=deepseek-chat-v3.1:free
LLM_MAX_TOKENS=3000

# Опционально: Ollama для локального режима
OLLAMA_API_URL=http://localhost:11434/api/chat
OLLAMA_MODEL=llama2
```

5. **Примените миграции**
```bash
python manage.py migrate
```

6. **Создайте суперпользователя**
```bash
python manage.py createsuperuser
```

7. **Запустите сервер**
```bash
python manage.py runserver
```

Откройте браузер и перейдите на `http://127.0.0.1:8000/`

## 📖 Структура проекта

```
sb-finance-ai/
├── core/                    # Основное приложение
│   ├── models.py           # Модели данных (Income, Expense, Document, etc.)
│   ├── views.py            # Представления и API endpoints
│   ├── forms.py            # Django формы
│   ├── urls.py             # URL маршруты
│   ├── llm.py              # Интеграция с AI (OpenRouter/Ollama)
│   ├── ml/                 # Machine Learning модули
│   │   ├── predictor.py   # Классификатор расходов
│   │   ├── forecast.py    # Прогнозирование прибыли
│   │   ├── recommender.py # Система рекомендаций
│   │   └── train_classifier.py  # Обучение модели
│   ├── utils/              # Утилиты
│   │   ├── file_ingest.py # Импорт CSV/Excel
│   │   ├── analytics.py   # Финансовая аналитика
│   │   ├── anonymizer.py  # Анонимизация данных
│   │   └── export.py      # Экспорт данных
│   └── templates/          # HTML шаблоны
├── sb_finance/             # Настройки Django
│   ├── settings.py        # Конфигурация
│   └── urls.py            # Главные URL маршруты
├── static/                # Статические файлы (CSS, JS)
├── media/                 # Загруженные файлы и модели ML
├── requirements.txt       # Зависимости Python
├── manage.py             # Django management script
└── .env                  # Переменные окружения (не в git)
```

## 🔌 API Endpoints

### Финансовые данные
- `GET /records/` - Получить все записи (доходы, расходы, документы, события)
- `GET /api/dashboard/data/` - Данные для дашборда (графики, статистика)
- `GET /income/` - Список доходов
- `GET /expense/` - Список расходов
- `POST /income/create/` - Создать доход
- `POST /expense/create/` - Создать расход

### AI и чат
- `POST /ai/chat/` - Отправить сообщение в AI-чат
- `GET /api/chat/sessions/` - Список сессий чата
- `GET /api/chat/sessions/<session_id>/` - История конкретной сессии
- `POST /api/chat/sessions/<session_id>/delete/` - Удалить сессию
- `GET /ai/insights/` - Получить AI-рекомендации

### Импорт и файлы
- `POST /api/upload/` - Загрузить файл (CSV, Excel, PDF, DOCX)
- `GET /api/files/` - Список загруженных файлов
- `POST /api/files/<file_id>/delete/` - Удалить файл
- `POST /api/files/<file_id>/delete-transactions/` - Удалить все транзакции из файла
- `POST /api/files/delete-transactions/` - Массовое удаление транзакций

### Дубликаты
- `GET /api/duplicates/find/` - Найти дубликаты транзакций
- `POST /api/duplicates/delete/` - Удалить выбранные дубликаты

### Настройки
- `GET /api/user/settings/` - Получить настройки пользователя
- `POST /api/user/settings/` - Сохранить настройки
- `POST /api/delete-all/` - Удалить все данные пользователя
- `GET /api/export/all/` - Экспортировать все данные

## 🎯 Основные функции

### Импорт данных из файлов

Поддерживаются форматы: **CSV**, **Excel (.xlsx, .xls)**

**Формат CSV/Excel:**
```csv
type,date,amount,category,description
income,2025-11-01,50000,services,Консультация клиента
expense,2025-11-02,15000,marketing,Реклама в соцсетях
```

**Возможности:**
- Автоматическое определение дубликатов при импорте
- Привязка каждой транзакции к файлу-источнику
- Фильтрация транзакций по файлам
- Массовое удаление транзакций из файлов
- Настройки автоматической очистки при повторном импорте

### AI-консультант

Интеллектуальный финансовый консультант на базе OpenRouter:
- Анализ финансовых данных
- Персонализированные рекомендации
- Прогнозирование трендов
- Обнаружение аномалий
- История диалогов с возможностью экспорта

**Поддерживаемые модели:**
- `openai/gpt-4o-mini` (рекомендуется)
- `google/gemini-pro-1.5-flash`
- `mistralai/mistral-7b-instruct`
- `qwen/qwen-2.5-7b-instruct`
- И другие модели OpenRouter

### Machine Learning

**Автокатегоризация расходов:**
```bash
# Обучить модель
python core/ml/train_classifier.py
```

Модель автоматически определяет категорию расхода по описанию.

**Прогнозирование прибыли:**
Линейная регрессия на основе исторических данных для прогноза следующего месяца.

### Управление дубликатами

- **Автоматическое обнаружение** при импорте
- **Визуальный интерфейс** для просмотра дубликатов
- **Массовое удаление** с подтверждением
- **Фильтрация по файлам** для поиска дублей в конкретном файле

## 🔐 Безопасность

### Шифрование
- Все данные могут шифроваться на клиенте (AES-256-GCM)
- Сервер не хранит ключи шифрования
- Экспорт в зашифрованном формате `.sbenc`

### Анонимизация
Автоматическое удаление персональных данных перед отправкой в облако:
- ФИО
- Номера карт и счетов
- Телефоны и email
- Адреса
- ИНН, СНИЛС

### Локальный режим
Полная работа без интернета с использованием Ollama:
```bash
# Установите Ollama
# https://ollama.ai/

# Запустите модель
ollama pull llama2

# Включите локальный режим в настройках
```

## 🛠️ Разработка

### Запуск тестов
```bash
python manage.py test
```

### Создание миграций
```bash
python manage.py makemigrations
python manage.py migrate
```

### Сбор статических файлов
```bash
python manage.py collectstatic
```

### Отладка LLM
```bash
python core/llm_debug.py
```

## 📦 Зависимости

Основные библиотеки:
- `Django>=4.2,<5.1` - Web framework
- `pandas>=2.0` - Обработка данных
- `scikit-learn>=1.3` - Machine Learning
- `matplotlib>=3.8` - Графики
- `requests>=2.31.0` - HTTP запросы
- `python-docx>=1.1.0` - Работа с DOCX
- `pdfminer.six>=20231228` - Работа с PDF
- `openpyxl>=3.1.0` - Работа с Excel
- `python-dotenv>=1.0` - Переменные окружения

Полный список в `requirements.txt`

## 🌐 Деплой

### Production настройки

1. **Обновите `.env`:**
```env
DJANGO_DEBUG=0
DJANGO_SECRET_KEY=your-production-secret-key
DJANGO_ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
```

2. **Настройте базу данных** (PostgreSQL рекомендуется):
```python
# sb_finance/settings.py
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'dbname',
        'USER': 'dbuser',
        'PASSWORD': 'dbpass',
        'HOST': 'localhost',
        'PORT': '5432',
    }
}
```

3. **Соберите статику:**
```bash
python manage.py collectstatic --noinput
```


## 🤝 Вклад в проект

Приветствуются Pull Requests! Пожалуйста:
1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request


**Сделано с ❤️ by Konuktor**
