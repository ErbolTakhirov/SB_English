# 🚀 БЫСТРЫЙ СТАРТ: Новая AI-система

## За 5 минут до запуска

### 1. Убедитесь что установлены зависимости ✅

```bash
pip install -r requirements.txt
```

### 2. Добавьте новый URL в `core/urls.py` ✅

```python
from core.ai.views import ai_chat_api_v2

urlpatterns = [
    # ... существующие
    path('api/ai/chat/v2/', ai_chat_api_v2, name='ai_chat_v2'),
]
```

### 3. (Опционально) Добавьте модель Goals в `core/models.py` 

Скопируйте класс `FinancialGoal` из `core/ai/goals.py` в `core/models.py`

Затем:
```bash
python manage.py makemigrations
python manage.py migrate
```

### 4. Протестируйте систему 🧪

```bash
python test_ai_system.py
```

### 5. Используйте в коде 🎯

**Вариант 1: Используйте новый API endpoint**

JavaScript:
```javascript
fetch('/api/ai/chat/v2/', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
        'X-CSRFToken': getCookie('csrftoken')
    },
    body: JSON.stringify({
        message: "Как улучшить мою финансовую ситуацию?",
        session_id: currentSessionId
    })
})
.then(r => r.json())
.then(data => {
    console.log('Query Type:', data.query_type);
    console.log('Response:', data.reply);
    console.log('Advices:', data.actionable_by_section);
})
```

**Вариант 2: Используйте напрямую в Python**

```python
from core.ai.advisor import get_financial_advice

result = get_financial_advice(
    user=request.user,
    query="Почему выросли расходы на маркетинг?",
    session=chat_session,  # опционально
    use_local=False,
    anonymize=True
)

print(result['response'])
print(result['query_type'])  # 'anomalies'
```

---

## 📊 Что дает новая система?

### До:
```
USER: "Почему так много трат?"
AI: "У вас высокие расходы. Рекомендую сократить траты."
```

### После:
```
USER: "Почему так много трат на маркетинг в ноябре?"

AI анализирует:
✅ Тип запроса: anomalies
✅ Категория: маркетинг  
✅ Период: ноябрь
✅ Собирает: транзакции, аномалии, тренды

AI отвечает:
## 🎯 БЫСТРЫЙ ВЫВОД
Расходы на маркетинг выросли на 47% в ноябре 
(с 15,000 до 22,000) из-за запуска новой кампании.

## 🔍 АНОМАЛИИ
- 15.11: 12,000 на "Яндекс.Директ" (обычно ~5,000)
- 22.11: 7,500 на "Instagram Ads" (обычно ~3,000)

## 💡 РЕКОМЕНДАЦИИ

### 🔥 СЕЙЧАС:
1. Проанализировать ROI кампаний (ожидаемый доход vs 22,000)
2. Установить лимит 18,000/месяц на маркетинг

### 📆 В ЭТОМ МЕСЯЦЕ:
1. Внедрить approval process для трат >10,000
2. Настроить автоматические уведомления при превышении бюджета

## 📈 ПРОГНОЗ
При текущих темпах расходы на маркетинг достигнут 
25,000/месяц через 3 месяца.
```

---

## 🎨 Основные фичи

### 1. **Умный анализ запросов**
Система понимает:
- ❓ Что спрашивает пользователь (тренды/аномалии/советы)
- 📅 Про какой период (последний месяц, год, конкретная дата)
- 💰 Про какие категории (маркетинг, офис, еда)
- 🔢 Какие суммы упомянуты

### 2. **Персонализированный контекст**
На основе типа запроса подбирает:
- 📊 Финансовые таблицы (только нужные месяцы)
- 📈 Тренды и динамику
- 🔍 Конкретные транзакции
- 👤 Профиль поведения пользователя

### 3. **Структурированные советы**
Советы группируются:
- 🔥 **СЕЙЧАС** - что делать на этой неделе
- 📆 **ЭТОТ МЕСЯЦ** - среднесрочные задачи
- 🔮 **ДОЛГОСРОЧНО** - стратегия на 3-6 месяцев

По приоритетам:
- 🚨 **СРОЧНО** - критические проблемы
- ⚡ **QUICK WIN** - быстрые победы
- 📅 **ДОЛГОСРОЧНО** - стратегические цели

### 4. **Отслеживание прогресса**
Система запоминает:
- Какие советы были даны
- Какие выполнены
- Completion rate (процент выполнения)
- Историю типов запросов

---

## ⚙️ Настройки

### Изменить модель LLM

В `.env`:
```env
# Для бесплатной модели
LLM_MODEL=deepseek-chat-v3.1:free

# Для более качественной (платной)
LLM_MODEL=openai/gpt-4o-mini

# Или локально через Ollama
OLLAMA_MODEL=llama2
```

### Кастомизировать промпт

В `core/ai/advisor.py` найдите `ADVANCED_FINANCIAL_PROMPT` и измените:

```python
ADVANCED_FINANCIAL_PROMPT = """
Ты — финансовый советник...

# ДОБАВЬТЕ СВОИ ПРАВИЛА:
- ...
- ...
"""
```

---

## 🐛 Troubleshooting

### Проблема: "ModuleNotFoundError: No module named 'core.ai'"

**Решение:**
Убедитесь что создан файл `core/ai/__init__.py`:
```bash
touch core/ai/__init__.py
```

### Проблема: "AI ошибка: HTTP 401"

**Решение:**
Проверьте API ключ в `.env`:
```env
LLM_API_KEY=sk-or-v1-ваш-ключ-здесь
```

### Проблема: "Query Analyzer не определяет тип"

**Решение:**
Добавьте больше ключевых слов в `query_analyzer.py`:
```python
KEYWORDS = {
    QueryType.TRENDS: [
        'тренд', 'динамик', 'изменени',
        # ДОБАВЬТЕ СВОИ:
        'развитие', 'прогресс', 'движение'
    ],
}
```

### Проблема: "Контекст слишком большой"

**Решение:**
Уменьшите `max_context_size` в `context_builder.py`:
```python
def build(self, query_analysis, max_context_size=5000):  # было 10000
```

---

## 📚 Полная документация

См. [AI_ENHANCEMENTS.md](./AI_ENHANCEMENTS.md)

---

## ✅ Checklist перед production

- [ ] Протестирован на разных типах запросов
- [ ] Проверена работа с локальной моделью (Ollama)
- [ ] Настроены лимиты токенов для экономии
- [ ] Добавлен rate limiting (max N запросов/час)
- [ ] Проверена анонимизация персональных данных
- [ ] Настроен мониторинг (Sentry/логи)

---

**🎉 Готово! Теперь ваш финансовый AI стал намного умнее!**
